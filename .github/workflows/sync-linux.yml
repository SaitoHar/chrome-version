name: Sync Linux Releases

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      versions:
        description: Exact Chrome versions to build, comma separated. Leave as-is to build everything above min_version.
        required: false
        default: 149.0.7827.200,150.0.7871.186
      min_version:
        description: Oldest Chrome version to build, ignored when `versions` is set
        required: false

concurrency:
  group: sync-linux

permissions:
  contents: write

jobs:
  ubuntu:
    runs-on: ubuntu-latest

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc
          df -h /

      - name: Yarn Install
        run: yarn
        env:
          HERO_SKIP_CHROME_DOWNLOAD: true

      - name: Synchronize Versions File
        run: node ./scripts/syncVersions.js
        env:
          SYNC_OS_KEYS: linux
          SYNC_VERSIONS: ${{ github.event.inputs.versions }}
          SYNC_MIN_VERSION: ${{ github.event.inputs.min_version || vars.SYNC_MIN_VERSION || '148.0.0.0' }}
          SYNC_MAX_RELEASES: 10
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          branch: main
          commit_message: Sync Versions Cleanup
